SessionConfig = {}

SessionConfig.Matchmaking = {
    enabled = true,
    minPlayers = 1,
    maxPlayers = 4,
    queueTickMs = 1500,
}

SessionConfig.Extractions = {
    pointsPerSession = 3,
    zoneHoldMs = 10000,
}

SessionConfig.DeathDrops = {
    enabled = true,
    ttlSeconds = 900,
}
