SessionConfig = {}

SessionConfig.Matchmaking = {
    enabled = true,
    minPlayers = 1,
    maxPlayers = 4,
    queueTickMs = 1500,
}

SessionConfig.Extractions = {
    pointsPerSession = 3,
}

SessionConfig.DeathDrops = {
    enabled = true,
    ttlSeconds = 900,
}
