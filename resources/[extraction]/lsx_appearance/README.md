# lsx_appearance

Optional adapter for `illenium-appearance`.

This resource keeps LSX as the owner of character data. If `illenium-appearance` is installed and started, `lsx_appearance` opens its customization UI, receives the resulting appearance, and saves it back through `extraction_character`.

If `illenium-appearance` is not installed, LSX continues to work with the simple male/female operator flow.
